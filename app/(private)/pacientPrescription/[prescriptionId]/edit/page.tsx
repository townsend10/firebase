import { PrescriptionEdit } from "../_components/prescription-edit";

const PrescriptionEditPage = () => {
  return (
    <PrescriptionEdit content="xx" name="dasdsada" date={new Date()} days={0} />
  );
};

export default PrescriptionEditPage;
